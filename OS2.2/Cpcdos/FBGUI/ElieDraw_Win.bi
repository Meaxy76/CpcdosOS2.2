
' Design top left
Dim shared design_Image0 as string

' Design top right
Dim shared design_Image1 as string

' Design top middle
Dim shared design_Image2 as string

' Design bottom title bar
Dim shared design_Image3 as string

' Close button
Dim shared btn_close as string

' sizeup button
Dim shared btn_sizeup as string

' sizedown button
Dim shared btn_sizedown as string

' reduce button
Dim shared btn_reduce as string